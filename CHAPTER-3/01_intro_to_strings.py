name = "deep"
      # 0,1,2,3
shortname = name[0:2] # 1st index include and last index is not include
print(shortname)
character1 = name[0]
print(character1)




# ***Strings are immutable(change nahi hoti hai )***